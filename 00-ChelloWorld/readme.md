# TP 0 - ChelloWorld

Este trabajo práctico consiste en un programa escrito en C que imprime un saludo por pantalla. La salida estándar del programa se redirige a un archivo de texto utilizando la consola.

## Archivos involucrados

- `hello.c`: archivo fuente del programa.
- `output.txt`: archivo generado con la salida del programa.

## Descripción

Dentro de `hello.c` se encuentra un programa simple que imprime un saludo dirigido al alumno **Julián Colman**. Al ejecutar el programa y redirigir su salida con el operador `>`, se genera un archivo `output.txt` que contiene el resultado.

